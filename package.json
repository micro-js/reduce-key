{
  "name": "@micro-js/reduce-key",
  "description": "Reduce a keyed map (or array), using a selector function.",
  "repository": "git://github.com/micro-js/reduce-key.git",
  "version": "1.0.2",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@micro-js/clone-shallow": "^1.0.0",
    "@micro-js/is-undefined": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
